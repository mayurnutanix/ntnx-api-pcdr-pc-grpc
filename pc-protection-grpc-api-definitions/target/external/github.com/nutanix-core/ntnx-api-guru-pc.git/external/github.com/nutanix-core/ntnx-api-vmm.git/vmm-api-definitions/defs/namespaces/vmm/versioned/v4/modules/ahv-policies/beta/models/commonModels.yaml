components:
  schemas:
    HostReference:
      description: DocRef(PolicyHostReferenceDescription)
      type: object
      properties:
        extId:
          type: string
          description: DocRef(PolicyHostExtIdDescription)
          x-constraints:
            pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-codegen-hint:
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: extId
                  index: 2001
    CategoryReference:
      description: DocRef(PolicyCategoryReferenceDescription)
      type: object
      properties:
        extId:
          type: string
          description: DocRef(PolicyCategoryExtIdDescription)
          x-constraints:
            pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-codegen-hint:
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: extId
                  index: 2001
    UserReference:
      description: DocRef(PolicyUserReferenceDescription)
      type: object
      properties:
        extId:
          type: string
          description: DocRef(PolicyUserExtIdDescription)
          x-constraints:
            pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-codegen-hint:
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: extId
                  index: 2001
    VmReference:
      description: DocRef(VMReferenceDescription)
      type: object
      properties:
        extId:
          type: string
          description: DocRef(VMExtIdDescription)
          x-constraints:
            pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-codegen-hint:
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: extId
                  index: 2001
    ClusterReference:
      description: DocRef(PolicyClusterReferenceDescription)
      type: object
      properties:
        extId:
          type: string
          description: DocRef(PolicyClusterExtIdDescription)
          x-constraints:
            pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-codegen-hint:
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: extId
                  index: 2001