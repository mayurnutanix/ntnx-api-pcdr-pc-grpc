paths:
  /vms/{vmExtId}/gpus/{extId}:
    x-backend-service-name: pcVmmSvc
    get:
      tags:
        - "ApiEndpoint(Vm)"
      description: DocRef(GetGpuDescription)
      summary: DocRef(GetGpuSummary)
      operationId: "getGpuById"
      x-rate-limit:
        type: pc
        config:
          - type: xsmall
            count: 10
            timeUnit: seconds
          - type: Small
            count: 20
            timeUnit: seconds
          - type: Large
            count: 25
            timeUnit: seconds
          - type: XLarge
            count: 25
            timeUnit: seconds
      x-permissions:
        operationName: View_Virtual_Machine_GPU
        description: "Allows to view a Virtual Machine GPU."
        serviceName: VMM
        entityType: vm
        deploymentList:
          - ON_PREM
          - CLOUD
        roleList:
          - Account Owner
          - Administrator
          - User
          - Consumer
          - Developer
          - Internal Super Admin
          - Operator
          - Prism Admin
          - Prism Viewer
          - Project Admin
          - Self-Service Admin
          - Super Admin
          - VPC Admin
          - Storage Admin
          - Network Infra Admin
          - Virtual Machine Admin
          - Virtual Machine Operator
          - Virtual Machine Viewer
        legacyParentOperationList:
          - VMM:View_Virtual_Machine
        legacyOperationList:
          - Prism:View_Virtual_Machine_GPU
      parameters:
        - name: vmExtId
          in: path
          description: DocRef(VmExtIdDescription)
          required: true
          schema:
            type: string
            x-constraints:
              pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
        - name: extId
          in: path
          description: DocRef(GpuExtIdDescription)
          required: true
          schema:
            type: string
            x-constraints:
              pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-codegen-hint:
        spring-mvc:
          - type: non-blocking
            properties:
              non-blocking: true
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: "vmExtId"
                  index: 2001
                - name: "extId"
                  index: 2002
        proto:
          - type: tag
            properties:
              value: VMs
          - type: operationId
            properties:
              value: getGpuByExtId
          - type: responseModelName
            properties:
              value: GetGpuResponse
      x-supported-versions:
        - product: PC
          version: 2024.1
        - product: PE
          version: 6.8
      responses:
        200:
          description: DocRef(GetSuccess)
          content:
            application/json:
              schema:
                $ref: "ModelRef({/namespaces/vmm/versioned/v4/modules/ahv/beta/models/Gpu})"
        x-api-responses:
          template: "ext:common:/namespaces/common/versioned/v1/modules/response/beta/models/apiResponse"
          responseModelName: "GetGpuApiResponse"
        x-codegen-hint:
          $any:
            - type: entity-identifier
              properties:
                identifiers:
                  - type: "ModelRef({/namespaces/vmm/versioned/v4/modules/ahv/beta/models/Gpu})"
                    index: 2001

    delete:
      tags:
        - "ApiEndpoint(Vm)"
      description: DocRef(DeleteGpuDescription)
      summary: DocRef(DeleteGpuSummary)
      operationId: "deleteGpuById"
      x-rate-limit:
        type: pc
        config:
          - type: xsmall
            count: 5
            timeUnit: seconds
          - type: Small
            count: 10
            timeUnit: seconds
          - type: Large
            count: 15
            timeUnit: seconds
          - type: XLarge
            count: 15
            timeUnit: seconds
      x-permissions:
        operationName: Delete_Virtual_Machine_GPU
        description: "Allows to delete a Virtual Machine GPU."
        serviceName: VMM
        entityType: vm
        deploymentList:
          - ON_PREM
          - CLOUD
        roleList:
          - Account Owner
          - Administrator
          - User
          - Consumer
          - Developer
          - Internal Super Admin
          - Operator
          - Prism Admin
          - Project Admin
          - Self-Service Admin
          - Super Admin
          - Virtual Machine Admin
        legacyParentOperationList:
          - VMM:Update_Virtual_Machine_GPU_List
          - VMM:Update_Virtual_Machine
        relatedOperationList:
          - VMM:View_Virtual_Machine_GPU
        legacyOperationList:
          - Prism:Delete_Virtual_Machine_GPU
      parameters:
        - name: vmExtId
          in: path
          description: DocRef(VmExtIdDescription)
          required: true
          schema:
            type: string
            x-constraints:
              pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
        - name: extId
          in: path
          description: DocRef(GpuExtIdDescription)
          required: true
          schema:
            type: string
            x-constraints:
              pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-codegen-hint:
        spring-mvc:
          - type: non-blocking
            properties:
              non-blocking: true
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: "vmExtId"
                  index: 2001
                - name: "extId"
                  index: 2002
        proto:
          - type: tag
            properties:
              value: VMs
          - type: operationId
            properties:
              value: deleteGpu
          - type: responseModelName
            properties:
              value: DeleteGpuResponse
      x-required-headers:
        - name: NTNX-Request-Id
          description: DocRef({ext:common:/namespaces/common}, requestIdHeaderDesc)
        - name: If-Match
          description: DocRef({ext:common:/namespaces/common}, ifMatchHeaderDesc)
      x-supported-versions:
        - product: PC
          version: 2024.1
        - product: PE
          version: 6.8
      responses:
        202:
          description: DocRef(Accepted)
          content:
            application/json:
              schema:
                $ref: "ModelRef({ext:tasks:/namespaces/prism/versioned/v4/modules/config/beta/models/TaskReference})"
        x-api-responses:
          template: "ext:common:/namespaces/common/versioned/v1/modules/response/beta/models/apiResponse"
          responseModelName: "DeleteGpuApiResponse"
        x-codegen-hint:
          $any:
            - type: entity-identifier
              properties:
                identifiers:
                  - type: "ModelRef({ext:tasks:/namespaces/prism/versioned/v4/modules/config/beta/models/TaskReference})"
                    index: 2001

  /vms/{vmExtId}/gpus:
    x-backend-service-name: pcVmmSvc
    get:
      tags:
        - "ApiEndpoint(Vm)"
      description: DocRef(ListGpusDescription)
      summary: DocRef(ListGpusSummary)
      operationId: "listGpusByVmId"
      x-rate-limit:
        type: pc
        config:
          - type: xsmall
            count: 2
            timeUnit: seconds
          - type: Small
            count: 3
            timeUnit: seconds
          - type: Large
            count: 5
            timeUnit: seconds
          - type: XLarge
            count: 7
            timeUnit: seconds
      x-permissions:
        operationName: View_Virtual_Machine_GPU
        description: "Allows to view a Virtual Machine GPU."
        serviceName: VMM
        entityType: vm
        deploymentList:
          - ON_PREM
          - CLOUD
        roleList:
          - Account Owner
          - Administrator
          - User
          - Consumer
          - Developer
          - Internal Super Admin
          - Operator
          - Prism Admin
          - Prism Viewer
          - Project Admin
          - Self-Service Admin
          - Super Admin
          - VPC Admin
          - Storage Admin
          - Network Infra Admin
          - Virtual Machine Admin
          - Virtual Machine Operator
          - Virtual Machine Viewer
        legacyParentOperationList:
          - VMM:View_Virtual_Machine
        legacyOperationList:
          - Prism:View_Virtual_Machine_GPU
      parameters:
        - name: vmExtId
          in: path
          description: DocRef(VmExtIdDescription)
          required: true
          schema:
            type: string
            x-constraints:
              pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      x-api-features:
        - ApiFeature(PARAMETERS, {ext:common:/namespaces/common/versioned/v1/modules/config/beta/models/Page})
      x-codegen-hint:
        spring-mvc:
          - type: non-blocking
            properties:
              non-blocking: true
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: "vmExtId"
                  index: 2001
                - name: "$page"
                  index: 2002
                - name: "$limit"
                  index: 2003
        proto:
          - type: tag
            properties:
              value: VMs
          - type: operationId
            properties:
              value: listGpus
          - type: responseModelName
            properties:
              value: ListGpusResponse
      x-supported-versions:
        - product: PC
          version: 2024.1
        - product: PE
          version: 6.8
      responses:
        200:
          description: DocRef(GetSuccess)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "ModelRef({/namespaces/vmm/versioned/v4/modules/ahv/beta/models/Gpu})"
        x-api-responses:
          template: "ext:common:/namespaces/common/versioned/v1/modules/response/beta/models/apiResponse"
          responseModelName: "ListGpusApiResponse"
        x-codegen-hint:
          $any:
            - type: entity-identifier
              properties:
                identifiers:
                  - type: "ModelRef({/namespaces/vmm/versioned/v4/modules/ahv/beta/models/Gpu})"
                    container: "array"
                    index: 2001

    post:
      tags:
        - "ApiEndpoint(Vm)"
      description: DocRef(CreateGpuDescription)
      summary: DocRef(CreateGpuSummary)
      operationId: "createGpu"
      x-rate-limit:
        type: pc
        config:
          - type: xsmall
            count: 5
            timeUnit: seconds
          - type: Small
            count: 8
            timeUnit: seconds
          - type: Large
            count: 20
            timeUnit: seconds
          - type: XLarge
            count: 20
            timeUnit: seconds
      x-permissions:
        operationName: Create_Virtual_Machine_GPU
        description: "Allows to create a Virtual Machine GPU."
        serviceName: VMM
        entityType: vm
        deploymentList:
          - ON_PREM
          - CLOUD
        roleList:
          - Account Owner
          - Administrator
          - User
          - Consumer
          - Developer
          - Internal Super Admin
          - Operator
          - Prism Admin
          - Project Admin
          - Self-Service Admin
          - Super Admin
          - Virtual Machine Admin
        legacyParentOperationList:
          - VMM:Update_Virtual_Machine_GPU_List
          - VMM:Update_Virtual_Machine
        relatedOperationList:
          - VMM:View_Virtual_Machine_GPU
        legacyOperationList:
          - Prism:Create_Virtual_Machine_GPU
      parameters:
        - name: vmExtId
          in: path
          description: DocRef(VmExtIdDescription)
          required: true
          schema:
            type: string
            x-constraints:
              pattern: "ConstantRef({ext:common:/namespaces/common}, uuidPattern)"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "ModelRef({/namespaces/vmm/versioned/v4/modules/ahv/beta/models/Gpu})"
      x-codegen-hint:
        spring-mvc:
          - type: non-blocking
            properties:
              non-blocking: true
        $any:
          - type: entity-identifier
            properties:
              identifiers:
                - name: "vmExtId"
                  index: 2001
                - name: "body"
                  index: 2002
        proto:
          - type: tag
            properties:
              value: VMs
          - type: responseModelName
            properties:
              value: CreateGpuResponse
      x-required-headers:
        - name: NTNX-Request-Id
          description: DocRef({ext:common:/namespaces/common}, requestIdHeaderDesc)
        - name: If-Match
          description: DocRef({ext:common:/namespaces/common}, ifMatchHeaderDesc)
      x-supported-versions:
        - product: PC
          version: 2024.1
        - product: PE
          version: 6.8
      responses:
        202:
          description: DocRef(Accepted)
          content:
            application/json:
              schema:
                $ref: "ModelRef({ext:tasks:/namespaces/prism/versioned/v4/modules/config/beta/models/TaskReference})"
        x-api-responses:
          template: "ext:common:/namespaces/common/versioned/v1/modules/response/beta/models/apiResponse"
          responseModelName: "CreateGpuApiResponse"
        x-codegen-hint:
          $any:
            - type: entity-identifier
              properties:
                identifiers:
                  - type: "ModelRef({ext:tasks:/namespaces/prism/versioned/v4/modules/config/beta/models/TaskReference})"
                    index: 2001
