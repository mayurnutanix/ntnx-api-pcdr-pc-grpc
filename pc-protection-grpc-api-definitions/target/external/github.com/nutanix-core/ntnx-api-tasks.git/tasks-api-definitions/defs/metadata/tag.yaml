tags:
  - name: Tasks
    description: >
      Tasks are primarily a way of asynchronously executing operations. Tasks persistently record the intent, 
      progress, and other relevant information about the long-running operations. Users can interact with 
      tasks in the system using these APIs.