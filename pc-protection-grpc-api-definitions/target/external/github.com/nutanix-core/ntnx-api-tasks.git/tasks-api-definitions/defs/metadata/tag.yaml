tags:
  - name: Tasks
    description: >
      Tasks are primarily a way of asynchronous execution of operations. Tasks record the intention, progress,
      and other relevant information about the long-running operations in a persistent manner.
      Users can interact with the tasks on the system using these APIs.